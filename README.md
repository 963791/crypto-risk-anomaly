# Crypto Risk & Anomaly (backend scaffold)

## Quickstart (development)
1. Copy `.env.example` -> `.env` and set values.
2. Build & run with Docker (from repo root):
   ```bash
   docker-compose -f docker/docker-compose.yml up --build
